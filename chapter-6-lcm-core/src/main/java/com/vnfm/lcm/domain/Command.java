package com.vnfm.lcm.domain;

/**
 * Marker interface for all domain commands.
 * Commands represent intent to perform an action (e.g. instantiate or terminate a VNF).
 */
public interface Command {
}
