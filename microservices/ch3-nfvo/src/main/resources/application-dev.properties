# -----------------------------------------------------------------
# PROFILE: dev (spring.profiles.active=dev)
#   Loaded in addition to application.properties; overrides same keys.
#   Use for local development when VNFM runs on the same machine (e.g. port 8081).
# -----------------------------------------------------------------
# Overrides vnfm.service.url so NFVO calls localhost instead of K8s DNS name.
vnfm.service.url=http://localhost:8081
