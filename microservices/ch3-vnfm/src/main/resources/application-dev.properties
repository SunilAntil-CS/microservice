# -----------------------------------------------------------------
# PROFILE: dev — Run VNFM on port 8081 so NFVO (dev profile) can call localhost:8081
#   Overrides server.port. Use: mvn spring-boot:run -Dspring-boot.run.profiles=dev
# -----------------------------------------------------------------
server.port=8081
